import 'package:flutter/material.dart';

class WaffleTab extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("WAFFLE TAB"),),
    );
  }
}