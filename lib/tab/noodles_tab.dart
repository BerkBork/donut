import 'package:flutter/material.dart';

class NoodlesTab extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("NOODLES TAB"),),
    );
  }
}