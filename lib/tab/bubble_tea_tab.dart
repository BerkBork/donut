import 'package:flutter/material.dart';

class BubbleTeaTab extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("BUBBLE TEA TAB"),),
    );
  }
}